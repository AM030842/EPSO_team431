<%= form_with(model: alum) do |form| %>
  <% if alum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alum.errors.count, "error") %> prohibited this alum from being saved:</h2>

      <ul>
        <% alum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :degree %>
    <%= form.text_field :degree %>
  </div>

  <div class="field">
    <%= form.label :divsion %>
    <%= form.text_field :divsion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

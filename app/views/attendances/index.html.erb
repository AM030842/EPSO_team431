<p id="notice"><%= notice %></p>

<h1>Attendances</h1>

<table>
  <thead>
    <tr>
      <th>Meeting</th>
      <th>Mem</th>
      <th>Att time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @attendances.each do |attendance| %>
      <tr>
        
        <td><%= meeting.where(id: attendance.Meeting_id).last.Meeting_title %></td>
        <td><%= member.where(id: attendance.Mem_id).last.Mem_name %></td>
        <td><%= attendance.Att_time %></td>
        <td><%= link_to 'Show', attendance %></td>
        <td><%= link_to 'Edit', edit_attendance_path(attendance) %></td>
        <td><%= link_to 'Destroy', attendance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Attendance', new_attendance_path %>

<br>

<%= link_to 'View All Meetings', meetings_path %>

<br>

<%= link_to 'Member List', members_path %>

<br>

<%= link_to 'Alumni List', alums_path %>

<br>

<%= link_to 'HomePage', '/' %>


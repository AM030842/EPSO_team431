<%= form_with(model: meeting) do |form| %>
  <% if meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :meeting_id %>
    <%= form.number_field :meeting_id %>
  </div>

  <div class="field">
    <%= form.label :meeting_title %>
    <%= form.text_field :meeting_title %>
  </div>

  <div class="field">
    <%= form.label :meeting_time %>
    <%= form.datetime_select :meeting_time %>
  </div>

  <div class="field">
    <%= form.label :meeting_desc %>
    <%= form.text_area :meeting_desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

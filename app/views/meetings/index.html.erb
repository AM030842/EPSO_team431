<!DOCTYPE html>
<html>
  <head>
    <title>Meeting List</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Warning !! ensure that "stylesheet_pack_tag" is used, line below -->
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="/">Home</a>
  <form class="form-inline">
    <a class="btn btn-outline-primary" type="button" href="/meetings/new">CREATE NEW MEETING</a>
    <a class="btn btn-outline-success" type="button" href="/attendances/new">GET POINTS</a>
    <a class="btn btn-outline-primary" type="button" href="/members">MEMBER LIST</a>
    <a class="btn btn-outline-success" type="button" href="/alums">ALUMNI LIST</a>
  </form>
</nav>
<p id="notice"><%= notice %></p>

<h1>Meetings</h1>

<div class="container" id="table_container">
<%= form_tag(meetings_path, method: :get) do %>
  <p> Search for a meeting by title</p>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag ("Search") %>
<% end %>
<br>
<table style="width: 80%;" class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th><%= link_to "Title", :sort => "title" %></th>
      <th><%= link_to "Time", :sort => "time" %></th>
      <th>Description</th>
      <th><%= link_to "Points", :sort => "points" %></th>
      <th colspan="3">Operation</th>
    </tr>
  </thead>

  <tbody>
    <% @meetings.each do |meeting| %>
      <tr>
        <td><%= meeting.title %></td>
        <td><%= meeting.time %></td>
        <td><%= meeting.description %></td>
        <td><%= meeting.points %></td>
        <td><%= link_to 'Show', meeting %></td>
        <td><%= link_to 'Edit', edit_meeting_path(meeting) %></td>
        <td><%= link_to 'Delete', meeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>